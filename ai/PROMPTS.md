# Prompts Used for AI-Assisted Work

This file records the prompts used to create the contents of the `ai/` folder for Assignment 5b.

## Prompt 1 — Planning

> In assignment 5b, create an R notebook using the dataset iris.csv from manual. Use the ml-r environment and check spelling. Build a notebook that loads it, splits train/test, trains a few classifiers, evaluates them, and picks the best. Reproducible env + README. Save as ai/notebook.ipynb, ai/environment.yml, ai/README_AI.md, ai/PROMPTS.md. Plan this out in plain English with each phase laid out. Do not delete branches, merge, commit, or work outside assignment5b.

**Outcome:** Explored the repo (`manual/iris.csv`, `manual/ml_r.ipynb`, `manual/environment.yml`), confirmed the `ml-r` conda environment, and produced a six-phase plan (setup, environment, notebook, documentation, QA, optional git).

## Prompt 2 — Recommendations

> What do you recommend? No git.

**Outcome:** Recommended five classifiers (LDA, CART, kNN, SVM, Random Forest), minimal EDA, portable `environment.yml` without a machine-specific prefix, and saving the notebook with outputs after one successful run.

## Prompt 3 — Implementation

> OK

**Outcome:** Created `ai/environment.yml`, `ai/notebook.ipynb`, `ai/README_AI.md`, and this file. Ran the notebook in the `ml-r` environment and filled the README with actual results.
